version: '3.8'
services:
  iraq-bot:
    build: .
    container_name: iraq_data_bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Baghdad
